document.addEventListener("DOMContentLoaded", () => {
    const deleteBtn = document.getElementById('deltBtn')
    deleteBtn.addEventListener('click', () => {
        alert('hi');
    })
})

function onClick(){
    
}